$(function () {
    $('.owl-carousel').owlCarousel({
        loop:true, //是否循环
        items:1,   //份数
        autoplay:true,  //是否自动播放
        autoplayTimeout:3000, //切换的时间
        autoplayHoverPause:true //鼠标移上是否提示播放
        
      
    });
})